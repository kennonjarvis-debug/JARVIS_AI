This folder hosts static assets for the Jarvis web app.

Expected file:
- Jarvis.shortcut — the signed Apple Shortcuts file for Jarvis

Note: The current repository state does not include the real `Jarvis.shortcut`.
Once you have generated it from `Jarvis.shortcut.json`, replace the placeholder
file if present and ensure this directory includes the binary `.shortcut` file.

