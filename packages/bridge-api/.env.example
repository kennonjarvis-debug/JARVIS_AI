# JARVIS Bridge API Configuration

# Server Configuration
BRIDGE_PORT=5555
NODE_ENV=development

# Security - REQUIRED
# Generate a secure token: openssl rand -base64 32
BRIDGE_BEARER_TOKEN=your-secure-bearer-token-at-least-32-characters-long

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Command Whitelist (comma-separated)
# Leave empty to use defaults
ALLOWED_COMMANDS=

# Path Whitelist (comma-separated)
# For cloud deployment, use /app/repos paths
# For local development, use your Mac paths
ALLOWED_PATHS=/app/repos/JARVIS_AI,/app/repos/DAWG_AI,/tmp

# File Size Limit (bytes)
# Default: 10485760 (10MB)
MAX_FILE_SIZE=10485760
