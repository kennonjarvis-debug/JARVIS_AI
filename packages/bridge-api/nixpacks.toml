[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']

[phases.install]
cmds = ['npm install -g pnpm && npx pnpm install']

[phases.build]
cmds = ['npx pnpm build']

[start]
cmd = 'node scripts/clone-repos.js && node dist/index.js'
